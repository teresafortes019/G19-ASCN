---

- name: Service Ghost
  shell: kubectl apply -f roles/deploy-ghost/templates/ghost-service.yml

- name: Ghost Deployment
  shell: kubectl apply -f roles/deploy-ghost/templates/ghost-deployment.yml
